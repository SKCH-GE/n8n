# Postgres
POSTGRES_USER=n8n
POSTGRES_PASSWORD=changeme123
POSTGRES_DB=n8n

# Public webhook base: the ngrok URL you provided
WEBHOOK_URL=${HTTPS-URL-PortRedirection}

# Basic auth for n8n editor (recommended for exposed setups)
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=verystrongpassword

# (Optional) -- if you later want to run an ngrok container uncomment
# NGROK_AUTHTOKEN=your_ngrok_authtoken_here
# NGROK_HOSTNAME=reserved-host.ngrok.io
